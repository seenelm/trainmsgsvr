[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
mongodb = "2.8.0"
serde = { version = "1.0.195", features = ["derive"] }
chrono = { version = "0.4.31", features = ["serde"] }

database = { path = "../database" }